using System;

namespace wasabiName
{
  class wasabiClass
  {
    static void Main(string[] args)
    {
    }
  }
}

/* Above is the standard model for a C# programme. We have our namespace (wasabiName), which is a collection of classes, currently only one (wasabiClass)
   which is a collection of functions (methods) and definitions. Currently we only have one function, which is the Main function: a default function 
   always run in the programme, and hence has to be there */
